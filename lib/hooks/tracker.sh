#!/bin/bash

# dynamic tracker list
# use this script to set trackers or set them in deploy-system.conf
# gets a list of depots for host, depots are trackers
# if host is a tracker host, then all of trackers become host's peers


#echo sun
#echo tracker1.domainA
#echo tracker2.domainB
